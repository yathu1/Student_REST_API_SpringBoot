server:
  port: 1234
  error:
    include-message: always

spring:
  datasource:
    url: jdbc:postgresql://localhost:5442/student
    username: yathu
    password: password
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false